import { GooglepickerDirective } from './googlepicker.directive';

describe('GooglepickerDirective', () => {
  it('should create an instance', () => {
    const directive = new GooglepickerDirective();
    expect(directive).toBeTruthy();
  });
});

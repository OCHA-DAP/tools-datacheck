import { DropboxchooserDirective } from './dropboxchooser.directive';

describe('DropboxchooserDirective', () => {
  it('should create an instance', () => {
    const directive = new DropboxchooserDirective();
    expect(directive).toBeTruthy();
  });
});
